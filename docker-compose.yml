version: "3.8"

services:
  # Vite React App
  frontend:
    ports:
      - "5173:5173"
    expose:
      - 5173
    build:
      context: ./frontend
      dockerfile: Dockerfile
    volumes:
      - .:/frontend
    environment:
      - NODE_ENV=development  # Optional: Set to 'production' for optimized build
  
